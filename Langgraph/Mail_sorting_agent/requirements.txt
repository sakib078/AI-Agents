# Mail sorting agent — pinned to versions verified on Python 3.14.
# uv:  uv venv --python 3.14  &&  uv pip install -r requirements.txt

# --- LLM + agent framework ---
langchain==1.3.11
langchain-core==1.4.8
langchain-groq==1.1.3
langgraph==1.2.7
pydantic==2.13.4

# --- Chat UI ---
chainlit==2.11.0

# --- Gmail API (direct REST) ---
google-api-python-client==2.198.0
google-auth==2.55.1
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.0
httplib2==0.32.0

# --- config + optional tracing ---
python-dotenv==1.2.2
langfuse==4.13.0        # optional — only used by the notebook's Langfuse cell